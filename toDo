1. add Cache:  -users ???
               -restaurants realized
               -dishes??
               -menus??
               -votes??
      ehcache.xml

2. SpringSecurity

5. should methods MenuService, DishService (restaurantId, menuId ...)

6. VoteTo правильно ли использовать поля restaurantName и userName, или надо restaurant и user

7. Vote должно ли быть menuId в соответствии с rest иерархией

8. AdminController create создает только юзера Role.USER, те либо подавать не UserTo, a User, либо разные методы
    надо ли делать метод createAdmin
    update не может изменить роль юзера

9. AdminMenuController надо ли принимать в теле запроса MenuTо, было бы достаточно просто даты
    MenuTo поле restaurantId или restaurantName
    сделать метод getTodayMenu(int restaurantId)